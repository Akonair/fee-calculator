# 费率结算自动计算器（本地版）· 交付概览

## 交付物
`fee-calculator/index.html` —— 单文件本地应用，双击即可使用，零依赖、无需安装任何环境。

## 功能清单
1. **代收计算器（输入即算）**
   - 上游结算：金额 + 上游汇率 + 上游手续费% → **实收** = 金额 × 汇率 × (1 − 上游费率)
   - 下游结算：金额/汇率自动带出（只读）+ 我方手续费% → **实付** = 金额 × 汇率 × (1 − 我方费率)
   - **毛利** = 实收 − 实付，高亮显示（正绿负红），每项附公式注释
2. **货币系统（可自由扩展）**
   - 内置 EUR/USD/GBP/CHF/JPY/CNY/HKD/SGD/AUD/KRW，默认 EUR → KRW
   - 支持自定义添加任意货币（代码校验、去重），localStorage 持久化
   - KRW/JPY 取整显示，其他货币 2 位小数，千分位 + 货币符号
3. **记账系统**
   - 一键入账（可填备注，如商户名 EC Markets / ZeroMarkets）
   - 账本列表按时间倒序，单条删除/清空需二次确认，支持备注关键词 + 日期范围筛选
   - 汇总卡片：总笔数 / 总实收 / 总实付 / 总毛利，与筛选联动
   - CSV 导出（UTF-8 BOM，Excel 打开中文不乱码）
   - 数据存 localStorage，刷新不丢失
4. **输入校验**：金额/汇率须为正、费率 0–100，非法输入红框且禁用入账

## 质量验证
- 初版：QA 以无头 Chrome 实测 32 项断言全部通过（公式数字核对、货币切换与自定义、记账增删筛、CSV 导出、刷新持久化、控制台零 JS 报错）。
- USDT 增量变更：两轮回归。第 1 轮 27/29（发现毛利条静态文案残留 KRW 且输入未填完时不刷新），工程师修复后第 2 轮 **33/33 全部通过**，无回归、无遗留问题。

## 增量变更（v2）：结算货币改为 USDT
- 内置货币新增 **USDT（泰达币）**，按稳定币规则显示（2 位小数 + 千分位，如 USDT 1,421,000.00）
- **默认结算货币 KRW → USDT**（金额货币仍默认 EUR，即默认 EUR→USDT 结算）
- 记住用户上次选择的结算货币（localStorage），无历史选择时默认 USDT
- 账本列表、汇总卡片、CSV 导出全链路同步 USDT 格式；KRW/JPY 取整行为不变

## 增量变更（v3）：精度提升至 8 位小数
- **汇率**：显示从最多 4 位小数提升为最多 8 位（如 `1.16225213` 完整显示，不再截成 1.1623）
- **金额**：实收/实付/毛利从固定 2 位小数提升为最多 8 位（去掉多余尾零、保底 2 位），如 `USDT 1,139.0070874`；整数结果仍显示 `1,421,000.00`
- **内部计算**：归一化精度从 6 位提升到 8 位，计算链路（计算器、账本、汇总、CSV 导出）全量同步
- **KRW/JPY 取整行为不变**；千分位只分组整数部分（修复了小数超 2 位时可能被错误加逗号的隐患）
- 验证：纯函数模拟 + 无头浏览器实测 10/10 全 PASS（默认 USDT 回归、8 位精度、保底 2 位、KRW 取整、账本/汇总、控制台零报错）

## 增量变更（v4）：全量货币 + 搜索选择 + Gas Fee + 总计行
1. **内置 66 种货币**：覆盖全球主要货币（亚太/美洲/欧洲/中东非洲）+ USDT/USDC 稳定币，仍支持自定义添加
2. **货币搜索选择框**：原始货币与结算货币均改为可搜索下拉（按代码或中文名搜索，回车选第一个匹配），替代原生下拉
3. **Gas Fee 选项**：毛利区新增「商户承担 Gas Fee（实付另扣 1.2U）」勾选框——勾选后实付 = 金额×汇率×(1−我方费率) − 1.2，毛利相应增加 1.2；入账记录带「含Gas」标记，CSV 新增「商户承担Gas」列
4. **备注示例中性化**：占位文案改为「备注（选填，如：5月第1批结算）」，不再使用真实商户名
5. **汇率参考提示**：每个货币自带对 USD 的大概值，汇率输入框占位提示随货币对自动变化（如 EUR→USDT 显示「如 1.08（参考值，以实际为准）」、EUR→KRW 显示「如 1459.46」），不再固定为韩元风格示例
6. **账目总计（含筛选）**：账本表格底部新增总计行，汇总卡片全部跟随当前筛选；多币种记录按结算货币分组合计、互不混算（如「₩1,430,271 · USDT 1,139.01」）
- 验证：无头 Chrome 实测 19/19 全 PASS（搜索选中、Gas 计算精度、多币种总计、筛选联动、控制台零报错）；旧测试脚本 test.js / test_precision.js 的选择器已过时，以 test_v4.js 为准

## 增量变更（v5）：上下游汇率单价调整（±）
1. **新增调整输入框**：上游费率旁新增「上游汇率单价调整（每 1 EUR ± USDT）」，下游费率旁同样新增——费率不只是百分比，还可叠加汇率单价的增减
2. **三种用法全部支持**：
   - 纯百分比（调整留空，与旧版完全一致）
   - 百分比 + 单价调整：**实收 = 金额 × (汇率 + 调整) × (1 − 上游费率)**，实付同理
   - 零费率纯单价：费率留空 = 0，只靠单价加成结算（如汇率 1.16225213 + 0.01）
3. **动态公式注释**：结果区公式随输入实时变化，如「实收 = 金额 × (汇率 +0.5) × (1 − 上游费率)」，Gas 勾选时实付公式追加「− 1.2（Gas Fee 由商户承担）」
4. **校验**：调整值须为数字且「汇率 + 调整」须为正数，否则红框并禁用入账；支持 `+0.5` / `-0.3` / `0.5` 等写法，留空 = 0
5. **全链路同步**：入账记录保存调整值，账本费率列显示徽标（如 `2% +0.01`、`5% −0.02`），CSV 新增「上游单价调整 / 下游单价调整」两列，重置按钮一并清空
- 验证：纯函数逻辑模拟 4 组场景手算核对一致 + 无头 Chrome 实测 **34/34 全 PASS**（回归旧公式、正负调整、零费率纯单价、负毛利提示、非法/越界校验、入账徽标、刷新持久化、CSV 新列、控制台零报错）

## 增量变更（v6）：费后汇率展示 + 备注常用选项
1. **费后汇率（上下游）**：在费率与单价调整输入框下方新增展示框——**费后汇率 = (汇率 ± 单价调整) × (1 − 费率%)**，即每 1 原始货币实际结算的结算货币数量（如 `1.1488070874`），可直接报给上下游
   - 只读不可编辑，**点击即复制**（纯数字，含「已复制 ✓」反馈；clipboard API 不可用时自动降级）
   - 附换算式注释：`(1.16225213 +0.01) × (1 − 2%) = 1.1488070874 USDT`
   - 标签随货币对动态变化（如「上游费后汇率（每 1 EUR 实得 USDT）」）；无费率时 = 汇率 ± 调整；输入非法时显示 —
   - 精度扩展至 10 位小数（汇率 × 费率的乘积常超 8 位有效小数，8 位会截断）
2. **备注常用选项**：备注输入框旁新增「常用 ▾」菜单
   - 「＋ 将输入框内容存为常用」可把当前备注保存为常用项（自动去重，最新的排前面）
   - 点选常用项即自动填入备注输入框；每项可单独删除（×）
   - 常用项存 localStorage（`novolink_remark_presets_v1`），刷新不丢失；点击菜单外部自动收起
- 验证：无头 Chrome 实测 **32/32 全 PASS**（费后汇率数值与换算式、零费率/纯单价场景、复制反馈与复位、常用备注增删选/去重/排序/持久化、菜单交互、只读性、控制台零报错）；v5 全量回归 **34/34 通过**

## 增量变更（v7）：实收实付复制 + 结算规则套用 + Telegram 工单
1. **实收 / 实付点击复制**：实收、实付数字点击即复制整串显示文本（如 `USDT 1,148.8070874`），复制后绿色高亮反馈；显示 — 时不可复制
2. **结算规则套用设置**：计算器顶部新增规则栏——把当前「上游费率 + 上游单价调整 + 下游费率 + 下游单价调整」四个值存为命名规则（如「欧洲A方案」），之后「套用规则 ▾」下拉一键填回并重算
   - 以原始字符串保存，保留「留空 = 0」语义；重名覆盖；菜单项显示摘要（如 `上2%+0.01 / 下5%−0.02`）；可单独删除
   - localStorage 持久化（`novolink_rule_presets_v1`），刷新不丢失；点击菜单外自动收起
3. **上游 / 下游计算工单（Telegram 制式）**：实收、实付结果下方各新增「📋 生成工单」按钮，弹窗展示该方向结算工单并可一键复制全文：
   - 内容：时间 / 货币对 / 原始金额 / **原汇率** / **费率与单价调整** / 费后汇率 / **实际打款金额**（下游勾选 Gas 时附扣减说明行）
   - **Telegram 制式**：关键数据全部用反引号包裹——粘贴到 Telegram Desktop 发送时自动转为等宽代码块，移动端**点按数据即可复制**；分隔线 + 分段排版便于人阅读
   - 输入无效时点击弹「请先修正输入」提示
4. **入账记录全单工单**：账本每行新增「工单」按钮，产出该单完整信息工单——上游段（原汇率 / 费率 / 单价 / 费后汇率 / 实收）+ 下游段（同构 + Gas）+ 💰 本单毛利，同样 TG 制式可复制
- 验证：无头 Chrome 实测 **46/46 全 PASS**（复制行为、规则存取/套用/去重/持久化、两张方向工单全部关键行与数值基准、Gas 分支、入账全单工单、控制台零报错）；v5/v6 全量回归 **34/34 + 32/32 通过**，无破坏
- 已知显示细节：工单中整数金额按全站规则显示两位小数（如 `1,000.00 EUR`），保持与其他金额显示一致

## 增量变更（v8）：抬头批次 + 多金额多规则 + 工单代码块修复 + 导入导出
1. **工单抬头 / 批次（强制）**：计算器顶部新增「工单抬头（如：商户名）」与「批次号」输入框——生成工单与入账均**强制要求抬头**，未填写时弹窗拦截并聚焦；批次选填，随工单展示、随账目留档
2. **布局调整**：汇率移到上方（全局面板，标签内联货币对 + 原始货币可搜索选择框），金额移到下方
3. **动态多金额**：金额改为逐行输入——末行输入正数后**自动追加下一行**（不打断输入焦点）；输入 0 / 空不产生新行，无效行失焦自动移除；每行独立计算，结果区改为**小计实收 / 小计实付**
4. **多规则（业务）并行**：「＋ 添加下一规则」把当前规则（名称必填 + 费率/单价/金额/Gas）存入工单草稿，可连续添加多条；「套用前项汇率」勾选后添加时汇率保留不清空；草稿卡展示每条规则的小计，可单条移除或清空
5. **工单升级（三张）**：上游工单 / 下游工单 / 汇总工单——每规则一段（汇率 / 费率 / 单价 / 费后汇率 / 每笔明细 / 小计），末尾**总计**（N 项业务 · M 笔）；无草稿时按当前输入生成；账目「工单」按钮按工单号关联重建整单（含抬头 / 批次 / 规则 / 全部明细 / 总计 / 备注）
6. **Telegram 格式修复**：工单全文改为 ` ```text … ``` ` 三反引号代码块——粘贴到 Telegram Desktop 发送后渲染为等宽代码块，**点按 / 长按即可复制**（旧版单反引号包裹在 TG 中会原样显示且不可复制）
7. **记账关联**：入账按「每规则 × 每金额」拆条，每条携带抬头 / 批次 / 业务规则 / 规则序号 / 行号 / **工单号（ticketId）**；账本新增「抬头 / 批次 / 业务规则」三列，筛选关键词同时匹配备注 / 抬头 / 批次 / 规则 / 工单号；入账后自动清空草稿与批次、保留抬头
8. **导入导出**：顶栏新增「导出规则」（独立 JSON，与账目无关）、「导出账目」（JSON 备份）、「导入」（自动识别规则 / 账目 JSON：规则按名称去重合并，账目校验后追加）；CSV 新增抬头 / 批次 / 业务规则 / 工单号列，文件名与标题全部去除品牌标识
- Gas 口径说明：Gas Fee 按**笔**扣减（每笔金额 = 一次结算），工单明细、小计、账目三处口径一致
- 验证：无头 Chrome 实测 **56/56 全 PASS**（动态金额行为、多金额小计数值基准、抬头/规则名强制、套用前项汇率、三张工单代码块格式与总计数值、入账关联字段、账目工单整单重建、规则/账目/CSV 导入导出、套用规则填名、重置行为、控制台零报错）

## 增量变更（v9）：GitHub 云备份
1. **☁ 云备份按钮**（顶栏）：打开云备份设置弹窗——GitHub 用户名 / 仓库名 / 分支（默认 main）/ 云端文件路径（默认 `fee-calculator/data.json`）/ Token；配置仅存本机 localStorage（`novolink_github_config_v1`），重新打开弹窗自动回填
2. **立即备份**：把「账目 + 结算规则 + 自定义货币 + 常用备注 + 当前货币对」打包为 JSON（UTF-8 安全 Base64），通过 GitHub API 提交到**你自己的仓库**——云端无文件时新建，已有文件时带 sha 覆盖更新（相当于每次备份是一个 commit，可在 GitHub 上查看历史版本）
3. **从云端恢复**：读取云端备份 → 弹确认框（显示云端备份时间与笔数统计）→ 与本地**合并不删除**：账目按编号去重追加、规则同名覆盖、自定义货币按代码去重、常用备注去重，完成后显示新增/更新统计
4. **测试连接**：一键验证用户名 / 仓库 / Token 是否有效（区分公开 / 私有仓库）
5. **错误中文提示**：401 Token 无效、403 未勾选 Contents 权限、404 仓库/路径不存在、409 冲突等
6. **安全性**：数据存你自己的 GitHub 私有仓库，Token 只保存在本机浏览器、不经过任何第三方服务器；GitHub API 支持 CORS，`file://` 双击打开也能直连
- 使用步骤：① 在 GitHub 建一个**私有**仓库 → ② 生成 fine-grained PAT（只授权该仓库，勾选 Contents: Read and write）→ ③ 在弹窗填好 5 项配置 → 测试连接 → 立即备份
- 验证：无头 Chrome 实测 **29/29 全 PASS**（配置保存回填、无 Token 拦截、Base64 中文往返、连接测试、首次备份 GET404→PUT 无 sha、二次备份带 sha 覆盖、401/403 中文提示、恢复合并去重与持久化、云端异常提示）；v8 全量回归 **56/56 通过**，无破坏

## 增量变更（v10）：多商户工单 + TG 单数字复制 + 套用前项汇率 + 草稿一键入账
1. **套用前项汇率彻底修复**：勾选后**完全不需要手工输入汇率**——三条保险：① 勾选瞬间自动把上一条草稿的汇率填入输入框；② 汇率留空时校验/计算直接继承草稿前项汇率（不报红、可入账）；③ 添加下一条规则时自动继承。勾选状态跨规则保持，取消勾选则恢复正常校验
2. **商户移到规则侧（多商户并行）**：顶部改为「我方抬头（选填，仅入账/汇总工单使用）」；规则栏新增「下游商户」输入框（如 EC Markets）——每个商户一条条添加业务规则，草稿列表按「商户 · 笔数 · 汇率」展示，各商户互不冲突
3. **上游工单纯业务化**：只含规则/汇率/费率/明细/小计/总计，**不含商户名与我方抬头**（上游无需知道我的商户）
4. **下游工单按商户分段**：每个商户独立成段（双分隔线 + 🏢 商户头），段内为该商户的规则明细与「商户小计实付」，互不混排；总计行注明商户数——可直接整段复制发给对应商户
5. **汇总工单（内部）**：带我方抬头/批次，每个商户段含 实收/实付/毛利 小计，总计含全部商户汇总——给你自己看
6. **Telegram 工单重新设计（数字点按复制）**：**每个数字单独用单反引号包裹**（如 `` `1,723.2106311` `` USDT、`` `5`%``），货币符号/百分号留在反引号外——TG 中点一下数字即可复制出**纯数字**；弃用三反引号代码块，改为纯文本逐行输出
7. **草稿一键批量入账**：草稿有内容时点「入账」= 全部规则一次性入账（每规则 × 每笔一条账目、统一工单号、商户随规则记账），成功提示含规则数/笔数/实收/实付与工单关联号——「一个商户的所有业务一次清晰快速入账」
8. **账本/导出/筛选全链路支持商户**：账本新增「商户」列（抬头之后）；CSV 新增「下游商户」列；筛选关键词同时匹配商户名
9. **我方抬头改为选填**：入账/工单不再强制要求抬头
10. **v10.1 修复：草稿存在时「一键入账」按钮被禁用** —— 添加草稿后当前汇率栏被清空（或当前输入不完整），recalc() 会把按钮置为 disabled，导致点不进去；已改为：只要草稿区有有效规则，无论当前输入是否完整，都启用「一键入账」并隐藏红色提示
- 验证：无头 Chrome 实测 v10 **48/48 全 PASS**（新增 T5.6/T5.7：草稿存在 + 当前输入留空时按钮仍启用并成功入账）；v8 回归 **57/57**、v9 回归 **29/29** 全 PASS，无破坏

## 增量变更（v10.2）：版本徽标 + 旧版工单排查结论
1. **排查结论（"很多数字按不了"）**：对当前版代码全链路核查 + 无头 Chrome 实测，上游/下游/汇总/账目四类工单中**每一个数字**（每笔金额、实收、实付、小计、总计、笔数、商户数）均已用单反引号包裹，复制路径原样带反引号——代码无遗漏。根因是**使用了旧版文件**：桌面旧 zip 内 index.html 为 03:17 版（2788 行，无 tkAmt），系「单数字反引号」改造完成前打包；从旧 zip 解压使用、或浏览器标签页为修复前打开（file:// 页面不自动重载 JS），即会生成部分数字无反引号的工单
2. **版本徽标（三处）**：页头标题旁蓝色徽标 `v10.2`、工单弹窗顶部提示条（含"数字均已反引号包裹、可 TG 点按复制"说明与旧版自检提示）、页脚版本号——打开页面即可确认是否新版
3. **zip 重打包**：桌面 `费率计算器.zip` 已更新为 v10.2 完整版（含 tkAmt ×14、appVerBadge、hasDrafts 修复）
4. **使用指引**：如工单数字不可点按，请先看页头是否显示 `v10.2` 徽标；无徽标 = 旧版，关闭旧标签页重新打开 `fee-calculator/index.html`（或 Cmd+R 刷新）即可
- 验证：v10 测试 **49/49**（新增 T1.3 版本徽标断言：页头徽标 + 页脚版本号）；v8 回归 **57/57**、v9 回归 **29/29** 全 PASS，无破坏

## 增量变更（v11）：中文 / English / Bahasa Melayu / Tiếng Việt 四语版本
1. **语言切换器**：页头版本徽标旁新增下拉选择器（中文 / English / Bahasa Melayu / Tiếng Việt），切换即时生效全界面，选择持久化到 localStorage（key `fcLang`），**默认中文**——老用户打开即为熟悉界面
2. **i18n 框架**：`I18N` 词典 248 词条 × 4 语 = **992 条**；`t(key)` / `tf(key, args)` 取词（中文兜底、{0} 占位符）；静态 HTML 用 `data-i18n` / `data-i18n-ph` / `data-i18n-title` 注解，切换语言时全量重渲染（汇率标签、账本表、草稿卡、弹窗、统计等）
3. **翻译范围**：全部界面文案（标签/按钮/面板/placeholder/账本 15 列表头/校验提示/成功提示/确认弹窗/toast）、**四类工单全文**（上游/下游/汇总/账本，发给各国商户直接出对应语言）、CSV 表头与文件名、GitHub 备份提示
4. **未动的部分**：计算逻辑、数据模型、已有 localStorage 数据 key 全部不变，**旧版本账本数据（含中文「代收」类型值、无 merchant 字段的旧条目）完全兼容**；emoji 与单反引号数字机制完整保留，四语下 Telegram 点按复制语义不变
5. 货币下拉中的货币中文名（如「泰达币」「日元」）属数据非界面文案，暂保留中文（主标签为货币代码 USDT/EUR，不影响使用）
- 验证：**224/224 全过** —— test_i18n（新建，42 项：四语切换/工单翻译/无中文残留/reload 持久化/四语全流程入账）+ test_edge_i18n（QA 新增，47 项：全量 93 元素中文残留扫描、工单反引号外无裸数字、旧数据兼容、编辑中切换语言不丢草稿、非法 fcLang 回退中文）+ 回归 test_v10 **49/49**、test_v8 **57/57**、test_v9 **29/29**；源码 Bug 0

## 团队流程（快速模式 ×5）
初版、USDT 变更、v7（复制/规则/工单）、v9（GitHub 云备份）与 v10（多商户工单）均按快速模式：齐活林（主理人）→ 寇豆码（工程师，实现/修复）→ 严过关（QA，验证）。v3–v6、v8 为最小改动增量，由助手直接修改并经自动化测试验证（v10 测试 48/48、v9 29/29、v8 57/57、v7 46/46、v6 32/32、v5 34/34 全 PASS）。

## 后续可选
- 如需增加「代付」计算器（费率 2.8% 场景）、多账本/多商户分组，可随时提需求增量开发。
